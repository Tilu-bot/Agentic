# Agentic - Python Dependencies
# Install with: pip install -r requirements.txt

# HuggingFace model inference – runs entirely in-process, no server needed.
# transformers 4.47+ brings Gemma 3, Llama 3.3, Phi-4, Qwen 2.5 chat templates
# and improved bfloat16 / flash-attention support.
transformers>=4.47.0
# PyTorch 2.5 adds torch.compile improvements and better MPS (Apple Silicon)
# support.  2.6 is the latest stable as of early 2026.
torch>=2.5.0
# accelerate 1.2 adds device_map="auto" improvements for multi-GPU and MPS.
accelerate>=1.2.0

# Optional: 4-bit quantization (GPU only, requires compatible CUDA/ROCm)
# bitsandbytes>=0.44.0

# HuggingFace Hub helpers (model card fetch, cache management in Settings)
huggingface_hub>=0.27.0
# Progress bars for model snapshot download percentage updates
tqdm>=4.67.0

# Async HTTP client for the web-fetch skill
httpx>=0.28.0
# Pillow for optional icon image support
Pillow>=11.0.0
# TLS certificate bundle for stable HTTPS verification
certifi>=2024.8.30
# Open-source DuckDuckGo search client (no API key)
duckduckgo-search>=7.0.0
# Renamed successor package for DuckDuckGo search
ddgs>=8.0.0

# Document reader skills (PDF, Excel, Word, PowerPoint)
pypdf>=5.1.0
openpyxl>=3.1.5
python-docx>=1.1.2
python-pptx>=1.0.2

# PyInstaller for the standalone desktop app bundle
pyinstaller>=6.11.0

# PyQt6 - Professional native desktop GUI (recommended over Tkinter)
PyQt6>=6.8.0
PyQt6-WebEngine>=6.8.0
